#ifndef CSVQ_MODELS_H
#define CSVQ_MODELS_H
#include <string>
#include <vector>

namespace models {
struct value {};
using row = std::vector<std::string>; // to replace with vector<value>
} // namespace models

#endif
